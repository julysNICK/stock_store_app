import 'package:flutter/material.dart';

Center create_account() {
  return const Center(
    child: Text(
      "Create Account",
      style: TextStyle(
        color: Color.fromRGBO(49, 39, 79, .6),
        fontWeight: FontWeight.bold,
      ),
    ),
  );
}
